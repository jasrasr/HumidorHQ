; HumidorHQ PHP-FPM configuration
; Registers a shutdown hook that creates one verified runtime backup after the
; first successful login of each local calendar day.
auto_prepend_file=auto-backup-prepend.php
